pipeline {
  agent any

  tools{
    maven "MAVEN_HOME"
  }

  environment {
    EMAIL_RECIPIENTS = 'mk.choumiyan@gmail.com'
  }

  stages {
      stage('Build') {
        steps {
          dir('backend') {
            sh 'mvn clean install'
          }
        }
      }
  }
}
