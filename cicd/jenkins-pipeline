pipeline {
  agent any

  environment {
    EMAIL_RECIPIENTS = 'mk.choumiyan@gmail.com'
  }

  stages {
      stage('Build') {
        steps {
          dir('backend-app/shop-manager') {
            sh 'mvn clean install'
          }
        }
      }
  }
}
